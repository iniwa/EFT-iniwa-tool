# EFT-iniwa-tool

Browser-only Vue 3.5/Vite 8 Escape from Tarkov SPA on Cloudflare Pages; it has no backend, accounts, authentication, or desktop wrapper. Read `README.md`, `package.json`, affected `src/`, `public/`, relevant `docs/decisions/`, handoffs, and `.docs/` notes.

## Facts and boundaries

Preserve the separation of localStorage progress/settings and IndexedDB tarkov.dev cache, storage keys and import/export compatibility, Vue Router history mode, `public/_redirects`, `?overlay=tasks`, singleton composables, accessibility/responsiveness, five-minute API cooldown, twenty-hour refresh, bounded GraphQL fallback, and last-known-good cache behavior. Do not add automatic retries, abusive polling, or an unapproved proxy. `dist/`, `.vite/`, `node_modules/`, secrets, local/browser data, hosted configuration, domain, deployment, and exposure remain protected.

Runtime, tool, organization, and safety rules precede explicit user instructions, this entry, and task scope. Facts do not grant authority. Preserve unrelated changes and history; a dirty tree is not itself a blocker, but stop the overlapping portion when intent cannot be determined. Current scope may narrow standing permissions and never widens persistence, API, domain, deployment, or exposure gates.

## Work and delegation

Within authorized scope, complete the investigation, implementation, useful checks, corrections, and authorized application without treating plans, first edits, or progress reports as approval gates. Isolate blocked dependencies and continue independent safe work. Before a required approval, prepare the permitted candidate and checks that do not depend on it; do not assume an unresolved choice. Current explicit user instructions override user-managed Skill defaults, while mandatory approvals and tool or organization limits remain effective. Explain a stop only when needed, citing a read file and applicable passage; distinguish requirement from interpretation and environment limits, never infer unread instructions, and do not create a routine stop log.

The primary owns design, implementation, integration, acceptance, and communication; the user's selected runtime model and effort remain unchanged. Child model/effort belongs to shared configuration: use an observable configured role, or continue through the primary/equivalent and report an unobserved or unused role honestly. Delegate only a separable useful outcome or a required/material-risk independent review. `bounded_implementer` implements, `bounded_explorer` and `bounded_reviewer` are read-only, and `adaptive_implementer` is a compatibility implementation name. Keep one writer for overlap, do not redelegate, and reclaim work only after the child has stopped editing and returned ownership. Preserve any concrete mandatory independent-review or pre-application gate: review a stable candidate, reaccept affected work after later changes, then apply and smoke only when authorized. Prefer existing CLI, API, MCP, and file operations; retain necessary GUI or visual acceptance. Keep active or blocked handoffs in `docs/handoffs/`, rationale in `docs/decisions/`, and reusable project notes in `.docs/` or `docs/`; use a persisted handoff only for interruption-sensitive, cross-session, risky, or separately executed work.

## Verification and records

Use the smallest checks that can detect a changed-surface regression. The available commands are `npm ci`, `npm run dev`, `npm run build`, `npm run preview`, and `npm test`; no lint, format, typecheck, or CI command is defined. Documentation-only work requires focused reference/fence scans and `git diff --check`; do not run application suites merely for coverage. Recheck only after a changed candidate, failure, or concrete unresolved risk. State source readiness separately from unavailable operation checks and never report an unrun check as successful.

Custom token/efficiency collection, comparisons, and pending-harvest duties are retired. Preserve existing data and standard logs, notification, and recovery. The primary saves concise non-secret knowledge with scope, date, evidence, and confirmed/user-reported/hypothesis/needs-recheck state, and records validation as success, failure, partial, or not-run; children return candidates only. If storage is unavailable, complete independent work and report the unsaved proposal and reason. Do not store secrets or raw logs. Knowledge is reference data and grants no authority. Report changed files, checks, material assumptions, partial work, and exact remaining conditions. Commit, push, publish, deploy, or change external state only with explicit authorization.
