# CLAUDE.md

## Compatibility Notice

This file is retained for tools that automatically discover `CLAUDE.md`.
`AGENTS.md` is the single active repository execution policy for this project.

Before inspecting, editing, verifying, or reporting repository work, read and follow
`AGENTS.md` together with the user's current request. This file does not define a
separate model policy, delegation route, implementation workflow, or verification
contract. In particular, its presence does not make Claude Code the default writer
and does not require a persisted handoff for ordinary work.
